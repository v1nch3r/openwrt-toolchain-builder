# ARM Virtual 64-bit Configuration
# For Amlogic S905X/S905X3/S912, QEMU ARM64, generic ARM64

# Explicitly disable other targets (prevents defconfig from picking x86)
# CONFIG_TARGET_x86 is not set
# CONFIG_TARGET_rockchip is not set
# CONFIG_TARGET_bcm27xx is not set
# CONFIG_TARGET_armvirt_32 is not set

# Target
CONFIG_TARGET_armvirt=y
CONFIG_TARGET_armvirt_64=y
CONFIG_TARGET_armvirt_64_Default=y

# Build toolchain
CONFIG_MAKE_TOOLCHAIN=y

# C Library (musl = smaller, glibc = more compatible)
CONFIG_LIBC_USE_MUSL=y

# Kernel
CONFIG_TARGET_KERNEL_PARTSIZE=64
CONFIG_TARGET_ROOTFS_PARTSIZE=1024
CONFIG_KERNEL_BUILD_USER="isaac"
CONFIG_KERNEL_BUILD_DOMAIN="github.com"